const { defaultRenderStart, loadSubApp } = require("../../../src");
describe("index.js", () => {
  describe("defaultRenderStart", () => {
    it("should TBD", () => {
      defaultRenderStart({});
    });
  });
});
